# Server
PORT=5000
NODE_ENV=development

# Allowed origin(s) for CORS — comma-separated. Include your deployed frontend URL in production.
CLIENT_ORIGIN=http://localhost:5173

# Gmail credentials
# NOTE: GMAIL_APP_PASSWORD is a 16-character App Password from Google Account → Security → 2-Step Verification → App passwords.
# Do NOT use your regular Gmail login password.
GMAIL_USER=tyroneraymateo0115@gmail.com
GMAIL_APP_PASSWORD=wmzq zxng rndp tgrd

# Where contact-form messages are delivered (defaults to GMAIL_USER if omitted).
CONTACT_TO=tyroneraymateo2003@gmail.com
